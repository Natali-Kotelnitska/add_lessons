// let promotionTimer = new Date('Oct 24, 2021 15:06:00');

// const par = document.querySelector('.js-timer');

// const promotionTimerID = setInterval(() => {
//   //   console.log("Let's go!");
//   let distance = promotionTimer - Date.now();

//   const days = Math.floor(distance / (1000 * 60 * 60 * 24));

//   const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));

//   const mins = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));

//   const seconds = Math.floor((distance % (1000 * 60)) / 1000);

//   par.innerHTML = `days: ${days} hours: ${hours} mins: ${mins} seconds: ${seconds}`;

//   if (distance < 0) {
//     clearInterval(promotionTimerID);
//     par.innerHTML = 'Свято настало!';
//   }
// }, 1000);
